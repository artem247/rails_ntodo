<%= form_with(model: [todo_list, todo_item], local: true, class: "user_form" ) do |form| %>
  <% if todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_item.errors.count, "error") %> prohibited this todo item from being saved:</h2>

      <ul>
        <% todo_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="deadline-form">
    <%= form.label :deadline %>
    <%= form.date_select :deadline,  class:"input_field" %>
  </div>

  <div >
    <%= form.submit class:"submit_btn" %>
  </div>
<% end %>
